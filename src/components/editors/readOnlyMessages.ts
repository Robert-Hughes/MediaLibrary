// Shared copy for read-only schema messaging.
// Centralized so the editor Save tooltip, context-menu Remove disabled
// tooltip, and NewPropertyDialog banner stay in sync.

export const READ_ONLY_TOOLTIP = "Tag is read-only per ExifTool schema";
export const READ_ONLY_REMOVE_TOOLTIP =
  "Tag is read-only per ExifTool schema — cannot be removed";
